@extends('admin.layout')
@section('content')<div class="actions"><h1 style="margin-right:auto">{{ $label }}</h1><a class="btn" href="{{ url('/admin/'.$type.'/create') }}">Add new</a></div><div class="admin-card"><table class="table"><thead><tr><th>Name</th><th class="hide-mobile">Details</th><th class="hide-mobile">Active</th><th></th></tr></thead><tbody>@forelse($items as $item)<tr><td><strong>{{ $item->title ?? $item->name ?? $item->label }}</strong></td><td class="hide-mobile">{{ $item->description ?? ($item->option_group ?? '') }}@if(isset($item->category))<br><small>{{ $item->category }}</small>@endif</td><td class="hide-mobile">{{ $item->is_active ? 'Yes' : 'No' }}</td><td class="actions"><a class="btn btn-muted" href="{{ url('/admin/'.$type.'/'.$item->id.'/edit') }}">Edit</a><form method="post" action="{{ url('/admin/'.$type.'/'.$item->id) }}">@csrf @method('DELETE')<button class="btn danger" onclick="return confirm('Delete this item?')">Delete</button></form></td></tr>@empty<tr><td colspan="4">No records yet.</td></tr>@endforelse</tbody></table></div>@endsection
