@extends('admin.layout')
@section('content')<h1>Quote orders</h1><div class="admin-card"><table class="table"><thead><tr><th>Customer</th><th class="hide-mobile">Project</th><th class="hide-mobile">Work</th><th class="hide-mobile">Details</th><th class="hide-mobile">Status</th><th>Actions</th></tr></thead><tbody>@forelse($orders as $order)<tr><td><strong>{{ $order->name }}</strong><br>{{ $order->email }}<br>{{ $order->phone }}</td><td class="hide-mobile">{{ $order->project_type }} · {{ $order->project_location }}<br>{{ $order->commercial_property_type }}</td><td class="hide-mobile">{{ $order->service }}<br>{{ $order->address }}<br>{{ optional($order->preferred_date)->format('d M Y') }}</td><td class="hide-mobile">{{ $order->estimated_area ? $order->estimated_area.' m²' : '—' }}<br>{{ $order->tile_size ?: '—' }}<br>{{ $order->note ?: 'No note' }}<br>{{ count($order->photos ?? []) }} photo(s)</td><td class="hide-mobile">{{ $order->status }}</td><td><a class="btn btn-muted" href="{{ route('admin.order.show', $order->id) }}">View</a></td></tr>@empty<tr><td colspan="6">No quote requests yet.</td></tr>@endforelse</tbody></table></div>@endsection
